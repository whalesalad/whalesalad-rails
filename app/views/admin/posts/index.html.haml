- provide :title, @title

%section.posts

  %ul.post-list
    - for post in @posts
      %li 
        %h4
          = link_to post, edit_admin_post_path(post), :class => 'post-title'
        %span.meta #{time_ago_in_words post.created_at} ago &mdash; #{l post.created_at, :format => :long}
    